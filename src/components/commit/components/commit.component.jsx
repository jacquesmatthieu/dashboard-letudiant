import React, { Fragment } from 'react';
import CommitCount from './commitCount.component';

const Commit = () => {
  return (
    <Fragment>
      <CommitCount />
    </Fragment>
  );
};

export default Commit;